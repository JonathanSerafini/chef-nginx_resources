<% @configs['addresses'].each do |address| %>
set_real_ip_from <%= address %>;
<% end %>
real_ip_header <%= @configs['real_ip_header'] %>;
real_ip_recursive <%= on_off @configs['real_ip_recursive'] %>;
