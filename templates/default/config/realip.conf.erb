<% @configs['addresses'].each do |address| %>
<%= nginx_param('set_real_ip_from', address) %>
<% end %>
<%= nginx_param('real_ip_header', @configs['real_ip_header']) %>
<%= nginx_param('real_ip_recursive', @configs['real_ip_recursive']) %>
